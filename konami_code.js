const code = [38, 38, 40, 40, 37, 39, 37, 39, 66, 65]

let index = 0;

const mocha = document.body('mocha')
mocha.addEventListener('keydown', e => console.log(e.which))

function init() {
  // Write your JavaScript code inside the init() function
  
}